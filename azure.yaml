name: exstore
services:
  web:
    project: backend/ExStore.API
    language: dotnet
    host: appservice
    hooks:
      prepackage:
        windows:
          shell: pwsh
          run: npm --prefix frontend ci; npm --prefix frontend run build
        posix:
          shell: sh
          run: npm --prefix frontend ci && npm --prefix frontend run build
